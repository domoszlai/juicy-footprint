apply plugin: 'scala'
apply plugin: 'application'

mainClassName = "HelloWorld"
    
if (!hasProperty('mainClass')) {
    ext.mainClass = 'Main'
}

dependencies {
    compile project(':footprint-library')
    compile 'org.scala-lang:scala-library:2.12.4'
}